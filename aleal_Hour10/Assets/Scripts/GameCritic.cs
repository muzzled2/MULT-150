using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameCritic : MonoBehaviour
{
    // It's a little difficult to get the balls into their respective zones. 

    // Increasing the size of the box colliders would make the game a bit easier, as sometimes the balls barely miss the mark.

    //Adding some obstacles could make the game a little more fun, especially if the chaos balls dealt damage to the player.

    //The balls richoteting off each other is fun, but getting the balls into their zones is tedious at the moment.


}   //Suggestions to be implemented

    // Button that allows you to replay the game

    // Changing the size/shape of the bumper

    // Adding Timer

    // Complex Shape Bumper
